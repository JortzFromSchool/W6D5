<h1> All the cats </h1>

<ul>
    <% @cats.each do |cat| %>
        <li><%= cat.id %></li>
        <li><%= cat.name%></li>
        <li><a href="<%= cat_url(cat) %>">Show Cat</a></li>
        <br>
    <% end  %>
</ul>